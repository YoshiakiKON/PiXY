# PiXY 操作マニュアル（日本語）

この文書は PiXY のインストール、起動、主要機能の操作手順、および各パラメータの詳細説明をまとめた日本語の取扱説明書です。論文本文で省略した詳細設定や実務上の注意点を含みます。

---

## 目次

- 概要
- インストール
- さくっと使ってみる（クイックスタート）
  - ImageMode（Scs_Pix 相当）
  - StageMode（Scs_XY 相当）
- 詳細パラメータ一覧と説明
- プロジェクトの保存・読み込み
- トラブルシューティング
- 図（スクリーンショット）

---

## 概要

PiXY は、顕微鏡画像上のピクセル座標を分析装置のステージ座標に変換する GUI ツールです。主なワークフローは以下の通りです。

1. 画像読み込み（ImageMode）→ 候補点（重心）抽出
2. 参照点（fiducial points）入力とステージ座標測定（StageMode）
3. 最小二乗法により変換（アフィン近似）を推定
4. 候補点をステージ座標へ変換して CSV/クリップボード出力

本マニュアルは、各ステップで使う UI 操作、各パラメータの意味、推奨値や注意点を記載します。

## インストール

- Python 3.8 以上を推奨。
- `requirements.txt` に記載の依存関係をインストールします（例）。

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python Main.py
```

Windows 向けには配布済みのスタンドアロン実行ファイルを使用できます（リリース版）。

## さくっと使ってみる（クイックスタート）

ここでは ImageMode → StageMode の順で基本手順を示します。

### ImageMode（事前画像での候補点抽出）

1. `New Project` をクリックするとファイル選択ダイアログが開き、画像を指定して読み込みます（`New Project` は画像読み込みのショートカットです）。
2. 既に解析済みのセッションを再開する場合は `Load Project` で `.pixy` を読み込みます（プロジェクトには処理パラメータと抽出結果が含まれます）。
3. `Number of Groups (K)`、`Grain Size Threshold` など主要パラメータを設定します。計算モードが `Auto` の場合はパラメータ変更時に自動で再計算が走ります。`Manual` モードを選んでいる場合は、画面上の手動再計算ボタン（`Recalculate` 相当）を押して処理を実行してください。
4. オーバーレイ上で抽出結果を確認し、必要に応じてパラメータを調整します。満足したら `Save Project` で `.pixy` を保存し、`Export Image` で重心や番号を重ねた画像を出力できます。

（図：ImageMode の画面例）

![PiXY GUI - ImageMode](documentation/images/fig_ui.png)

#### 推奨パラメータ（初期目安）
- Number of Groups (K): 3–6（試料の色相・コントラストにより調整）
- Grain Size Threshold（最小面積）: 10–50 px（ノイズ除去のため）
- 最大面積: 試料サイズに合わせて設定（大領域を除外したい場合）
- Boundary Offset / Neck Separation / Shape Complexity (Advanced): 接触粒子分離や偽領域除去に応じて変更
# PiXY 操作マニュアル（日本語）

---

## 表紙情報

- ソフト名: PiXY — Pixel to stage-XY Coordinate Converter
- バージョン: 1.2.5
- 著者: Yoshiaki KON
- リポジトリ: https://github.com/YoshiakiKON/PiXY
- Zenodo DOI: 10.5281/zenodo.18174474
- ライセンス: MIT
- 最終更新: 2026-02-18

---

## 概要

- 目的: 顕微鏡画像上の候補点（重心）と装置上の参照点を結び付け、画像座標を装置ステージ座標へ変換することで、装置稼働時間の短縮と再現性向上を支援する。
- 対象: 微小域分析オペレータ、試料解析担当者、装置管理者
- 主な機能: 画像読み込み、粒子検出（K-means + 連結成分）、参照点入力、アフィン変換推定、残差可視化、CSV/クリップボード出力
- 効果: ターゲッティング作業の前倒し、装置上での手作業削減、より多数点の統計的解析が可能に

---

## 導入（インストール・起動）

- 動作環境
  - OS: Windows 推奨（配布 EXE あり）。Linux/Mac はソース実行で動作する可能性あり。
  - Python: 3.8 以上（ソース実行時）
  - 必要ディスク/メモリ: 画像解像度に依存（大画像は多メモリ推奨）
- 依存パッケージ: `requirements.txt` を参照（PySide6, OpenCV, NumPy 等）
- インストール（ソース）

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python Main.py
```

- 配布 EXE の実行: ダウンロード後、右クリック→[プロパティ]→[ブロック解除]（必要なら）を行い実行
- 注意: Python 実行時は仮想環境を有効にし、`requirements.txt` のバージョンに合わせてください。

---

## クイックスタート（最短手順）

1. PiXY_v1XX.exeを実行
1. `New Project` をクリックし、解析対象の画像ファイルを選択して読み込む。
2. 右側のパラメータ（`Number of Groups (K)`, `Grain Size Threshold` など）を調整し，粒子の重心が検出されているか確認．
3. `Add Fiducial Point` をクリックして参照点（画像上）を選択し、装置上で同じ点のステージ座標をテーブルに入力する。3点以上入力し，残差を確認．残差が全体的に大きい場合には点数を増やす．特に大きい点がある場合には，その点を確認もしくは除外する．
5. 問題なければ `Export CSV` または `Copy to Clipboard` で座標を出力して装置へ渡す。

図: 典型的操作の流れ（Image → Fiducial → Transform → Export）

![PiXY Workflow](documentation/images/workflow_v2.svg)

---

## UI 概観

- メイン領域: 画像表示（中央）、左側: プロジェクト操作/画像サムネ、右側: 候補点・参照点テーブル
- 主要ボタン（ラベルは UI に表示される正確な文言）
  - `New Project`: 画像ファイルを選択して新規セッション/画像読み込みを行う。
  - `Load Project` / `Save Project`: `.pixy` プロジェクトファイル（パラメータ＋結果）を読み書きする。
  - `Export Image`（`Export Image` ボタン）: 現在のオーバーレイを元画像に合成して画像ファイルとしてエクスポートする。
  - `Export XYZ`（`Export XYZ` ボタン）: 検出された重心をステージ座標（X,Y,Z）へ変換して CSV 形式で保存する。
  - `Clipboard`（`Clipboard` ボタン）: 出力データをクリップボードへコピーし、装置ソフトへ即時貼り付けできる形式で提供する。
  - `Filter`（`Filter` ボタン）: グループフィルタのポップアップを開き、表示/出力対象のグループを選択する。
  - `Add Target`（`Add Target` ボタン）: 画像上でターゲット点を追加する補助機能（ターゲット登録など）
  - `Update u, v`（`Update u, v` ボタン）: 選択中のターゲット／参照点の画像座標（u,v）を現在の位置で更新する。
  - `Clear`（`Clear` ボタン）: 選択中のターゲット／一時的な選択をクリアする。
  - `Add Fiducial Point`（`Add Fiducial Point` / `btn_add_ref`）: 参照点登録モードに入り、画像上で参照点をクリックして登録する。
  - `Update XY`（`Update XY` / `btn_update_xy`）: 選択中の参照点に対してステージ座標（X,Y,Z）を更新するためのボタン。
  - `Clear`（参照テーブル用の `Clear` / `btn_clear_ref`）: 参照点テーブルの選択行を削除またはリセットする。
  - `Original` / `Posterized`（オーバーレイ切替）: ポスタライズ表示と元画像表示を切り替え、クラスタ分割の確認を行う。
  - `Boundary`（`Show` / `Hide` トグル）: 領域境界線の表示/非表示を切り替える。
  - `Coordinate`（`Image` / `Stage` トグル）: 表示座標系を画像座標／ステージ座標表示に切り替える。
  - `Flip`（`Normal` / `Flip` トグル）: 画像の左右反転（ビューに応じて Image/Stage 用トグルがある）。
  - `+` / `-`（各数値入力のナッジボタン）: 数値パラメータ（K, Min Area 等）を微調整するための増減ボタン。
  - `Image Rotate` スライダー: 画像を手動回転して参照点の位置合わせを補助するコントロール（スライダー右の角度表示と連動）。

- 表の列説明
  - `u, v`: 画像座標（ピクセル）
  - `Stage X, Stage Y, Stage Z`: 装置の座標（装置ごとの単位に従う）
  - `Res. X/Y/Z`: 各参照点の残差
- 表示モード
  - `Original` / `Posterized`：ポスタライズ表示でクラスタ分割の確認が可能
  - Flip 設定: 画像反転の自動/手動選択（装置表示と画像表示の差を補正）

---

## ワークフロー詳細

- Image（候補点生成）
  - 画像を読み込み、`Number of Groups (K)` でクラスタ数を指定し、K-means により色分割を実行
  - 連結成分解析で領域ごとの重心を計算し、候補点リストを生成
  - パラメータ変更時に自動または手動で再計算（`Auto` / `Manual` モード）
- Fiducial（参照点）入力
  - `Add Fiducial Point` で画像上の再同定可能な特徴点をクリックして登録
  - 装置側で同点を見つけて `Stage X/Y/Z` を入力
  - 参照点は非共線の 4 点以上を推奨（5 点以上で安定）
- Transform（変換推定）
  - 参照点ペアから 2D→3D のアフィン変換を最小二乗で推定
  - 左右反転モデルも試行し、残差が小さいモデルを採用
  - 各参照点の残差を確認し、外れ値は再同定または除外して再推定
- Export（出力）
  - 候補点を推定変換でステージ座標に変換して CSV/Clipboard 出力
  - 装置固有のインポート形式がある場合は、CSV の列順を確認して調整

---

## 機能別ガイド

- 粒子検出（K-means + 連結成分）
  - `Number of Groups (K)` の決め方: 色数に応じて 3–10 を試し、過分割を避ける
  - `Grain Size Threshold`: 最小面積を指定してノイズを除去
  - `Boundary Offset` 等で接触粒子の分割を調整
- 残差可視化
  - GUI の残差テーブルとヒストグラムで分布を確認
  - RMS または中央値＋MAD ベースの閾で外れ値を判定
- プロジェクトの再現性
  - `.pixy` にパラメータと結果を保存
  - 現行バージョンでは K-means の内部シードは固定（ユーザー設定不可）

---

## パラメータリファレンス（抜粋）

- `Number of Groups (K)` — K-means のクラスタ数。推奨 3–10。
- `Grain Size Threshold` — 最小領域面積（px）。推奨 10–50 px。
- `Maximum Area` — 最大領域面積。大きすぎる領域を除外。
- `Boundary Offset` / `Neck Separation` / `Shape Complexity` — 接触粒子分離の調整パラメータ。
- `Processing Image Size` — 処理用縮小画像のサイズ。GUI 応答性向上のため縮小して処理。
- `Calc Mode` — `Auto`（変更で自動再計算） / `Manual`（手動再計算）

---

## ファイルとフォーマット

- `.pixy` — JSON 形式のプロジェクトファイル（入力画像を base64 で埋め込み、処理パラメータ、抽出結果、参照点、推定変換を含む）
- `centroids_*.csv` — 出力 CSV（No, Group, Stage X, Stage Y, Stage Z）
- 入力画像形式: PNG/JPG/BMP 等。推奨は非圧縮または高品質 PNG

---

## トラブルシューティング / FAQ

- 抽出点が多すぎる/少なすぎる: `K`, `Grain Size Threshold`, `Boundary Offset` を調整。必要なら事前フィルタ（平滑化）を行う。
- 変換残差が大きい: 参照点を追加して分散を減らす、外れ値参照点を除外して再推定。
- GUI が遅い: `Processing Image Size` を小さく設定して再計算を行う。
- ログの確認: 実行時に標準エラー出力へトレースを出力します。詳細デバッグは `DEBUG` フラグを有効にしてください。

---

## 実践例（短いチュートリアル）

1. 例: 光学像（BSE）を読み込み、`K=5`, `Min area=20px` で候補点抽出
2. 5 点の参照点を選び装置上でステージ座標を取得
3. 変換推定で 50% の残差が X,Y,Z 各々 3–4 μm 内に入ることを確認
4. CSV を書き出し装置に貼り付けて実験を実行

---

## 高度な利用法（開発者向けメモ）

- バッチ処理: 現行 GUI 実装はインタラクティブ目的。コマンドライン自動処理は要開発。
- PyInstaller 再ビルド: `build_exe.ps1` を利用（Windows 環境で PowerShell 実行）。
- 主要モジュール: `Ui.py`（UI）, `CalcCentroid.py`（重心計算）, `Util.py`（共通処理）

---

## ライセンスと引用方法

- ライセンス: MIT
- 引用例: Y. KON, PiXY v1.2.5. Zenodo:10.5281/zenodo.18174474

---

## 変更履歴（抜粋）

- v1.2.5 (2026-02-18): Windows EXE 配布、UI フリップ処理改善、README 追加
- v1.2.3: 内部のバグ修正、参照点残差表示改善

---

## 付録

- 用語集: `fiducial`（参照点）, `centroid`（重心）など
- 参考文献: PiXY のアルゴリズムや依存ライブラリの主要引用（論文/ライブラリ）
- Issue 提出テンプレ: 再現手順、OS/Python バージョン、ログ出力を添付してください

---

## 付録：数式とアルゴリズム詳細

この付録では、論文本文から省略した主要な数式と、GUI の主要機能で用いるアルゴリズムのロジックをできるだけ詳しく記載します。

### K-means によるポスタライズ（クラスタ分割）

- 目的関数（最小化対象）
  $$J = \sum_{k=1}^{K} \sum_{x\in S_k} \|x - \mu_k\|^2$$
  ここで $x$ は画素の色ベクトル（例: RGB）、$S_k$ はクラスタ $k$ に割り当てられた画素集合、$\mu_k$ はクラスタ $k$ の中心（重心）です。

- 反復手順（一般的な Lloyd のアルゴリズム）
  1. 初期中心 $\{\mu_k\}$ を設定（当実装では内部 RNG を固定して初期化）。
  2. 割当てステップ: 各画素 $x$ を最も近い中心 $\mu_k$ に割り当てる（ユークリッド距離）。
     $$\text{assign}(x) = \arg\min_k \|x - \mu_k\|^2$$
  3. 更新ステップ: 各クラスタの中心を平均で更新する。
     $$\mu_k \leftarrow \frac{1}{|S_k|}\sum_{x\in S_k} x$$
  4. 収束条件: 割当てが変わらないか、中心の移動量が閾値以下、または最大反復回数到達で終了。

- 実装上の注意
  - 初期化は再現性のため固定シード（`cv2.setRNGSeed(12345)` 等）を使用することがある（現行バージョンでは固定）。
  - 画像全体での K の選択は経験則に依存するため、GUI で `Number of Groups (K)` をユーザが調整します。
  - ポスタライズはクラスタラベルを代表色で置換することで可視化します（代表色は各クラスタの平均色など）。

### 連結成分解析と重心計算

- 手順
  1. 二値化（ポスタライズ結果から対象クラスタを選択）
  2. ラベリング（4-近傍または8-近傍）で連結成分を抽出
  3. 各成分について面積（ピクセル数）、周囲長、モーメントを計算
  4. 重心 $(u,v)$ は一次モーメントから算出
     $$u = \frac{M_{10}}{M_{00}},\quad v = \frac{M_{01}}{M_{00}}$$
    ここで $M_{pq}=\sum x^p y^q$ は領域のモーメント、$M_{00}$ は面積です。

### サイズフィルター（面積による除外）

- ロジック
  - 各ラベル領域の面積 $A$ を計測し、閾値以下（`min_area`）または閾値以上（`max_area`）の領域を除外します。

- 疑似コード

```
for region in labeled_regions:
    A = region.area
    if A < min_area or (max_area is not None and A > max_area):
        discard region
    else:
        keep region
```

### ネックセパレーション（接触領域の分割）

- 目的: 接触している粒子群を個々の粒子に分離する。
- よく使われる手法（実装での選択肢）
  1. 距離変換 + マーカーベースのウォーターシェッド
     - 領域の距離変換を計算し、局所極大点をマーカーとして抽出
     - マーカーを初期ラベルとしてウォーターシェッドを適用し、接触領域を分割
  2. 凸包や形状指標に基づく分割
     - 凸包からの逸脱（ソリディティ低下）や円形性の低さを検出し、分割候補とする
  3. 縮小/膨張（モルフォロジー）で細いネック部分を検出し、そこで分割する

- 実装上のパラメータ
  - `Neck Separation` の強さ（小さな値は弱い分割、値を上げるほど aggressive に分割）

### 境界オフセット（Boundary Offset）

- 画面端やトリミング領域での不完全な粒子を除外するために、画像の周囲を所定ピクセル分トリミングして処理対象外にします。

### 形状複雑度（Shape Complexity / Compactness）

- 評価指標例
  - 周囲長 $P$ と面積 $A$ から円形度（circularity）を計算
    $$C = \frac{4\pi A}{P^2}$$
    値が 1 に近いほど円形（単純）で、小さいほど複雑（切れ込みや細長）を示す。
  - ソリディティ（領域面積 / 凸包面積）も複雑度指標として有用

### 座標変換（2D 画像座標 → 3D ステージ座標）の推定

- モデル（アフィン近似）
  画像座標 $(u,v)$ から装置ステージ座標 $(X,Y,Z)$ へのアフィン写像は次のように表せます
  $$
  \begin{bmatrix} X\\ Y\\ Z \end{bmatrix}
  =
  \begin{bmatrix}
  a_0 & a_1 & a_2\\
  b_0 & b_1 & b_2\\
  c_0 & c_1 & c_2
  \end{bmatrix}
  \begin{bmatrix} 1\\ u\\ v \end{bmatrix}
  $$
  ここでパラメータ行列の各列は定数項と $u,v$ に係る係数を含みます。

- 最小二乗解の導出
  - 参照点 $i$ が $N$ 個あり、それぞれ画像座標 $(u_i,v_i)$ と測定ステージ座標 $(X_i,Y_i,Z_i)$ を持つとする。
  - 行列 $A\in\mathbb{R}^{N\times 3}$ を $A_i=[1\; u_i\; v_i]$（各行）とし、$B\in\mathbb{R}^{N\times 3}$ を $B_i=[X_i\;Y_i\;Z_i]$ とすると、各出力座標ごとに線形回帰を行うことでパラメータ行列 $P\in\mathbb{R}^{3\times3}$ を求める。
    正規方程式で解くと
    $$P = (A^T A)^{-1} A^T B$$

- 残差と評価指標
  - 残差行列 $R = B - A P$ を計算し、各点の残差ベクトルのノルム $\|r_i\|$ を求める。
  - RMS（root mean square）や各軸ごとの RMS で全体の精度を評価する。

---

## 用語集（五十音順）

以下は本マニュアルで使用する主要用語の簡潔な定義です。五十音順にグループ化して列挙します。

【ア行】
- アフィン変換: 画像座標に対する線形変換＋平行移動のモデル。2D→3D の近似として本アプリで用いられる。

【カ行】
- K-means（ケー・ミーンズ）: 画像の色空間を K 個のクラスタに分割する代表的な非階層的クラスタリング手法。
- クラスタ / グループ: K-means によってまとめられた画素集合。

【サ行】
- 参照点（fiducial）: 画像上で再同定可能な特徴点で、装置側で計測したステージ座標と対をなす点。
- 残差（residual）: 推定された変換で予測した座標と測定座標との差。精度評価に用いる。

【タ行】
- トリミング / Boundary Offset: 画像周辺を所定ピクセルだけ処理対象から外す操作。

【ナ行】
- ネックセパレーション: 接触粒子の間にある細い領域（ネック）を検出し、粒子を分離するアルゴリズム。
- ノイズ: 小さな偽領域や誤検出。`min_area` によるサイズフィルターで除去する。

【ハ行】
- ポスタライズ（Posterize）: 画素をクラスタ代表色に置換して簡潔に表示する処理。
- 形状複雑度（Shape Complexity）: 周囲長と面積などから計算される形状指標（例: circularity, solidity）。

【マ行】
- 面積（Area）: ラベリングされた領域のピクセル数。サイズフィルタリングに使用。
- 重心（Centroid）: 領域の一次モーメントから算出される代表点（画像座標 $u,v$）。

【ラ行】
- 連結成分（Connected Component）: 二値化画像における連続する画素集合。ラベリングで識別される。

【ワ行】
- ワークフロー: ImageMode（候補点抽出）→ StageMode（参照点入力・変換推定）→ Export（出力）という本ソフトの基本手順。

---

ファイル生成日: 2026-02-18
