# PiXY Release Notes v1.5.0

Release date: 2026-08-05

## PiXYとは

PiXY は、顕微鏡画像上で選定した候補点（粒子重心・手動ターゲット）を、装置ステージ座標へ変換して分析装置に渡すためのデスクトップ GUI ツールです。

主な役割
- オフラインで候補点を準備し、装置占有時間を短縮する。
- 画像上の参照点（fiducial）とステージ座標を対応付け、座標変換を推定する。
- 変換後の座標を CSV / クリップボード経由で装置ワークフローへ受け渡す。

主な機能
- 粒子検出（K-means + 連結成分）による候補点抽出。
- 手動ターゲット追加と候補点テーブル編集。
- 参照点入力と残差確認による変換品質評価。
- 画像オーバーレイ表示、境界表示、グループ可視制御。
- 画像座標系とステージ座標系を切り替えながらの整合作業。

想定ユーザー
- 微小領域分析オペレータ
- 試料解析担当者
- 装置管理者

## v1.4.4 からの主な差分

- Online 時の画像上座標系トグル運用を復元し、`Image (u, v)` / `Stage (X, Y)` の明示ラベルに更新。
- 画像上トグルの配置を見直し、Online 整合作業時の操作フローを改善。
- Stage 表示時に軸向きトグル（`Right`, `Top`）が確実に表示されるように修正。
- 画像オーバーレイの点色を調整し、Fiducial と手動 Target の識別性を向上。
- v1.5.0 向けの公式ドキュメントセット（EN/JP マニュアル、Quick Manual、HTML）とメタデータを更新。

## 互換性

- `.pixy` プロジェクト互換性は維持。
- 基本ワークフロー（抽出 → 参照点入力 → 変換 → 出力）は継続。
