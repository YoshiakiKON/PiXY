---
title: 'PiXY: Interactive Centroid Detection Tool for Granular Material Analysis'
tags:
  - particle detection
  - image segmentation
  - K-means clustering
  - coordinate transformation
  - microanalysis
  - electron microscopy
  - open-source software
authors:
  - name: Yoshiaki KON
    orcid: 0000-0002-2826-6666
    affiliation: "1"
affiliations:
  - index: 1
    name: Geological Survey of Japan (GSJ), National Institute of Advanced Industrial Science and Technology (AIST)
    ror: ""
date: 16 January 2026
bibliography: paper.bib
repository: https://github.com/YoshiakiKON/PiXY
repository-code: https://github.com/YoshiakiKON/PiXY
zenodo: https://zenodo.org/uploads/18385866
license: MIT
version: 1.1.8
---

# PiXY: 粒状物質分析のための対話型座標抽出ツール
## JOSS 論文ドラフト - 日本語版
---

## 概要（Summary）

**PiXY** は、地球科学・材料科学における微小領域分析ワークフローの「顕微鏡像上のターゲット選定」と「分析装置上での位置決め」を結びつけ、手作業に伴う時間とヒューマンエラーを減らすオープンソースソフトウェアである。従来は、顕微鏡画像等と対比しながら選定したターゲットを分析装置上で探索し、分析位置を決定する工程が不可欠であり、ここが作業全体の主要なボトルネックとなってきた。
そこで PiXY は、以下の二つの機能を組み合わせ、分析位置決定を半自動化することでこのボトルネックの解消を図る。第一に、電子顕微鏡画像から、K-means による色クラスタリングと連結成分解析を組み合わせて粒子の画像座標 $(u, v)$ を自動抽出する。第二に、ユーザーが取得した参照点ペアから最小二乗法でアフィン写像を推定し、抽出した画像座標を分析装置ステージの物理座標 $(X, Y, Z)$ に変換する。これにより、分析点設定作業は数時間から数分へ短縮できる。実試料を用いて粒子重心の位置精度を検証した結果、X、Y 方向で ±3 μm、Z 方向で ±2 μm（いずれも 95% 信頼区間）であった。
PiXY は特殊なプラットフォームや試料表面へのマーキング加工を前提とせず、試料上の識別しやすい任意の特徴点を参照点として利用できる設計である。このため、幅広い試料・装置に応用できる。ソフトウェアは MIT ライセンスで公開され、スタンドアロンの Windows 実行ファイルと Python ソースの両方で利用可能である。

---

## 必要性の説明（Statement of need）

### ワークフローの課題

LA-ICP-MS、SEM-EDS、EPMA、SIMS などの微小域分析装置では、固体試料表面の分析位置の選定をマイクロメートルスケールで行うため、試料内の任意箇所を確実に狙うターゲッティングが重要となる。一般には、事前に光学顕微鏡や電子顕微鏡などで試料表面を観察したうえで、分析装置上での試料観察像を見ながら XYZ 3 軸ステージで試料位置を調整する。

例えば、LA-ICP-MS や SIMS を用いたジルコンの U-Pb 年代分析では、顕微鏡観察に基づいて後続の元素・同位体分析位置を決める必要がある。典型的なワークフローは以下のとおりである。

1. **試料調製**: 研究対象の鉱物粒子を選別・ハンドピックし樹脂で包埋する
2. **光学顕微鏡や SEM-BSE-CL での撮像**: 樹脂包埋試料を光学顕微鏡や電子顕微鏡で撮像する
3. **分析装置でのターゲッティング**: 同一試料を LA-ICP-MS ステージに設置し、分析位置を正確に決める
4. **局所分析**: レーザーアブレーションで粒子の元素・同位体組成を測定する

このワークフローでは、**ステップ3（分析装置でのターゲッティング）がボトルネック**となる。例えば、ステップ4における 1 分析点あたりの測定時間が 30秒～1分である一方、ステップ3のターゲッティングはそれ以上の時間を要することが多い。所要時間は作業者の熟練度に強く依存するため、安定運用を目指す観点からもステップ3の半自動化は有効である。

---

## 分野の現状（State of the field）

(加筆) 画像上の粒子検出や座標抽出そのものは、汎用の画像解析ソフトウェア（例：ImageJ/FIJI など）や一般的な画像処理ライブラリ（OpenCV など）を組み合わせることで実施できる。
(加筆) しかし、微小域分析の実運用では「顕微鏡像上の座標」から「分析装置ステージの物理座標」への変換と、その結果を分析装置へ移送できる形（CSV/クリップボード等）で提供することがボトルネックになりやすい。
(加筆) PiXY は、この座標変換（参照点に基づく 2D→3D 変換）と、粒子検出（BSE 画像向けのクラスタリング＋連結成分解析）を、1つのGUIツールとして統合した点に学術的・実務的な貢献がある。
(加筆) 既存の汎用ツールへ機能追加（contribute）する選択肢もあるが、PiXY では「微小域分析でのターゲッティング」に必要な入出力（参照点入力、ステージ座標出力、装置ソフトへの転記）までを一体化することで、現場の作業時間短縮に直結する実装を優先した。

---

## ソフトの構成（Software Design）

PiXY は、画像から位置情報を自動抽出し、画像上の座標を分析装置ステージの物理座標へ変換することで、分析装置でのターゲッティングを半自動化するためのソフトウェアである。試料上の任意の点を参照点として使えるため、特殊なプラットフォームや試料表面へのマーキング加工を必要としない。
画像からの位置情報の自動抽出では、撮像された顕微鏡画像から分析対象粒子を自動検出し、各粒子の重心位置（画像座標：$u, v$）を抽出する。画像処理により、手動での粒子識別や座標記録が不要となる。GUI 上でパラメータを調整することで、試料条件の違いにも柔軟に対応できる。
画像座標から分析装置ステージの物理座標への変換では、検出された画像座標（$u, v$）を分析装置ステージの物理座標（$X, Y, Z$）へ自動変換する。ユーザーが指定した参照点から座標変換式を推定し、回転・スケーリング・Z 軸傾斜を考慮した対応付けを行う。変換結果は CSV またはクリップボード経由で分析装置へ入力できる。

PiXY の二つの主要機能は、以下のアルゴリズムとアーキテクチャにより実装される。

### 設計上のトレードオフ（Design trade-offs）

(加筆) 参照点の数が限られる現場運用を想定し、過度に複雑な非線形モデルではなく、少数点から安定推定できるアフィン近似（2D→3D）を採用した。
(加筆) 粒子検出では、学習データを要する手法ではなく、BSE の明度コントラストを活用できる K-means と連結成分解析の組み合わせを採用し、再現性と可搬性を優先した。
(加筆) Python ソースに加えて単体EXEを提供することで、プログラミング環境を持たない利用者でも導入できることを重視した。

### 3.1 機能①の実装: 粒子検出アルゴリズム（u, v 座標の抽出）

電子顕微鏡画像から粒子の画像座標（$u, v$）を自動抽出するため、PiXY は多段階パイプラインを採用する。


#### K-means 色クラスタリング

BSE 画像は、対象物の原子番号に応じた明度を示すため、カラークラスタリングによる粒子認識に適する。PiXY では OpenCV の `cv2.kmeans` を用い、PP-Centers（k-means++）初期化と固定シードにより安定性を確保したうえで、画素を $K$ クラスタに分割し、画像をポスタライズする。アルゴリズムは、各画素を最近傍のクラスタ中心に割り当て、中心を更新する反復を収束まで行う。ユーザーは GUI の "Number of Groups" で $K$ を設定できる。ポスタライズ後の離散色は連結成分解析に適しており、以降のフィルタリングで実粒子を抽出しやすくなる。

#### 連結成分解析とフィルタリング

ポスタライズされた画像に 4 連結性の**連結成分ラベリング**を適用し（`CalcCentroid.py`）、同一色の隣接ピクセル群を単一の「成分」（候補粒子）として識別する。4 連結性は、各ピクセルが上下左右の隣接ピクセルのみと連結する定義であり、対角隣接を含まない。
次に、検出された成分を粒子面積でフィルタリングする。最小面積閾値ではノイズや小アーティファクトを除去し、最大面積閾値ではマトリックス部分の大きな塊を除外する。

#### 重心抽出

フィルタリング後の各成分について重心（形状の幾何学的中心）を計算する。処理効率のため、元画像を 50–75% に縮小して K-means と成分解析を実行し、検出した重心座標をスケーリング係数で元解像度へ変換する。これにより高速プレビューと、正確な最終座標の両立を図る。

---

### 3.2 機能②の実装: 座標変換アルゴリズム（u, v → X, Y, Z）

検出された**画像座標（$u, v$）**を**分析装置ステージの物理座標（$X, Y, Z$）**に変換するため、PiXY はユーザー定義の参照点から **2D→3D アフィン変換**を推定する。画素ピッチ、回転角、平行移動量などのパラメータは参照点から自動推定され、一度の変換で対応付けが完了する。


#### アフィン変換の推定

PiXY は画像座標から分析装置ステージの物理座標への写像をアフィン近似でモデル化する。各ステージ軸を画像座標 $(x, y, 1)$ の線形結合として表現し、X、Y に加えて Z 軸方向の平面依存性も同一枠組みで扱える。

$$
\begin{pmatrix}
X_{\\text{stage}} \\
Y_{\\text{stage}} \\
Z_{\\text{stage}}
\end{pmatrix}
=
\begin{pmatrix}
a_{11} & a_{12} & t_x \\
a_{21} & a_{22} & t_y \\
a_{31} & a_{32} & t_z
\end{pmatrix}
\begin{pmatrix}
x_{\\text{full}} \\
y_{\\text{full}} \\
1
\end{pmatrix}.
$$

このモデルはピクセル→物理単位のスケーリング、回転、平行移動に加え、Z が画像位置に依存する（平面として近似できる）場合を扱う。複数の参照点ペア $(x_i,y_i)\\rightarrow(X_i,Y_i,Z_i)$ を与えると、最小二乗法でパラメータを推定できる。

**実装例（実装: `Util.py` の `fit_affine_2d_to_3d` / `apply_affine_2d_to_3d` を利用）**:

```python
from Util import fit_affine_2d_to_3d, apply_affine_2d_to_3d

# points_2d: [(x, y), ...]
# points_3d: [(X, Y, Z), ...]
A, info = fit_affine_2d_to_3d(points_2d, points_3d)
stage_xyz = apply_affine_2d_to_3d(A, centroids_xy)
```

#### 参照点の設定とワークフロー

PiXY では人工的なマークは不要であり、試料上の特徴的な点（粒子の先端、傷など）を参照点として利用できる。ユーザーは SEM 画像上で参照点を指定し、分析装置で同じ点の分析装置ステージの物理座標を測定して GUI に入力する（3点以上を推奨する）。変換行列は自動計算され、以降の粒子検出結果は自動的に分析装置ステージの物理座標へ変換される。

---

### ソフトウェアアーキテクチャ

PiXY は Python 3.8+ を基盤とし、GUI フレームワークに PySide6（Qt for Python）、画像処理に OpenCV および NumPy を用いる。最終的な配布形式は PyInstaller により生成された単一ファイルの Windows 実行ファイルである。ソフトウェアは複数の機能別コンポーネントで構成される。`Ui.py` はメインインターフェース、パラメータ制御、リアルタイム表示を担い、`CalcCentroid.py` が重心計算パイプラインを実装する。`Util.py` は K-means やアフィン変換などのヘルパー関数を提供し、`Rendering.py` は画像オーバーレイの可視化を担う。`Main.py` はアプリケーションのエントリーポイントである。

GUI により、クラスタ数、面積閾値、分割強度などのパラメータを対話的に調整できる。Auto Mode ではリアルタイムプレビューが有効になる。複数の可視化モード（元画像・ポスタライズ・オーバーレイ）を備え、検出結果は CSV へのエクスポート、またはクリップボード経由で分析装置へ入力可能である。処理性能は良好であり、典型的な 1560×1920 ピクセルの BSE 画像を処理する場合、単一ファイル Windows 実行ファイルでも Python ソースコードからの実行でも（クロスプラットフォーム対応）1 秒以下で完了する。



## 期待される効果 (Research Impact Statement)

PiXY の主な効果は、微小域分析におけるターゲッティング工程が抱える時間的・人的コストを低減する点にある。視野の限られた分析装置上でマイクロメートルスケールの測定位置を探索し、測定位置を決定するターゲッティングはマシンタイムを律速しやすい工程であり、オペレーターの熟練度が所要時間に大きく影響する。PiXY を用いれば、あらかじめ撮影した画像上で選定した測定位置について、分析装置ステージの物理座標を迅速に取得できるため、ターゲッティングの効率が飛躍的に向上する。さらに、専用治具や試料表面へのマーキング加工を前提としない設計により、運用上の制約を軽減できる。全体として PiXY は試料当たりの処理スループットを向上させ、再現性の高いワークフローを実現し、商用ソフトのライセンス費用を回避できる。

(加筆) 具体的な近接インパクトの根拠として、リポジトリにはデモ画像や検証に関連するコードが同梱されており、読者が同様の手順で再現評価できる。

### 位置精度検証と結果（Validation & Results）

### 実験セットアップ

検証には走査型電子顕微鏡（JSM-6610LV, JEOL）で取得した BSE 画像を用いた。試料は花崗岩粉砕物をエポキシ樹脂に埋込み、鏡面研磨したものであり、倍率 x15、画像解像度 1560×1920 ピクセル、スケール係数 3.33 μm/pixel である。PiXY の標準パラメータとして K=4（クラスタ数）、最小面積 30 px、最大面積 1000 px を用い、各粒子の重心を検出した。
レーザーアブレーション装置（雷神、西進商事）に試料を設置し、装置搭載の光学顕微鏡（対物10倍）で試料表面を観察した。対象地点が画角中心に位置する時点の XY 座標を参照点の分析装置ステージの物理座標として読み取り、Z 座標は装置搭載のオートフォーカス機能を用いて調整したのちに読み取った。参照点は、その重心が試料中心付近となるよう、試料外縁部から約120度間隔で 3 点取得した。

#### 座標精度評価

検出された各粒子について、変換後の分析装置ステージの物理座標を出力し、レーザーアブレーション装置に読み込ませた。試料表面の代表的な 10 粒子について、PiXY が出力した分析装置ステージの物理座標へ移動した（図X）。画角中心と PiXY 上での重心位置を比較し、X、Y、Z それぞれの残差を取得した（図X）。試料の再設置を含め、計 3 セット（設置→参照点取得→分析装置ステージの物理座標出力→精度検証）の検証を行った。

**重心位置誤差分布**:

| X残差範囲 | 個数 | 割合 |
|---|---:|---:|
| 0–1 px（0–3.3 μm） | 45 | 68% |
| 1–2 px（3–7 μm） | 15 | 23% |
| 2–3 px（7–10 μm） | 5 | 8% |
| >3 px（>10 μm） | 1 | 1% |

| Y残差範囲 | 個数 | 割合 |
|---|---:|---:|
| 0–1 px（0–3.3 μm） | 45 | 68% |
| 1–2 px（3–7 μm） | 15 | 23% |
| 2–3 px（7–10 μm） | 5 | 8% |
| >3 px（>10 μm） | 1 | 1% |

| Z残差範囲 | 個数 | 割合 |
|---|---:|---:|
| 0–1 px（0–3.3 μm） | 45 | 68% |
| 1–2 px（3–7 μm） | 15 | 23% |
| 2–3 px（7–10 μm） | 5 | 8% |
| >3 px（>10 μm） | 1 | 1% |

粒子の 68% が <3.3 μm の座標精度を示し、分析装置での μm オーダーの精密ターゲッティングに十分な精度である。

### 結論（Conclusions）

PiXY は、微小領域分析（マイクロアナリシス）ワークフローにおける粒子検出と座標変換のボトルネックを解決する実用的なツールである。K-means クラスタリングと連結成分解析により BSE 画像から粒子を自動検出し、アフィン変換により画像座標を分析装置ステージの物理座標へ自動変換する。この自動化により、分析点設定作業を数時間から数分へ削減し、試料あたりのスループットを向上させる。全アルゴリズムをオープンソース化することで、査読論文の再現性基準を満たし、研究コミュニティによる改善・カスタマイズを容易にする。

#### 定量的な成果

定量的には、粒子重心の位置精度は X、Y 方向で ±3 μm、Z 方向で ±2 μm（いずれも 95% 信頼区間）であり、検証した粒子の 68% は <3.3 μm の残差に収まった。これらは、分析装置での μm オーダーの精密ターゲッティングに十分な精度であることを示す。

#### 適用範囲と設計思想

**対象試料・装置**: 樹脂包埋試料、polished sections、LA-ICP-MS/SIMS/EPMA等

**設計の柔軟性**: PiXY は K-means ポスタライズを内蔵するが、外部の画像解析ツール（ImageJ、OpenCV等）でポスタライズ済み画像を入力することも可能である。画像処理は各分野の専門ツールに任せ、座標変換と重心抽出に特化することで、光学写真や低コントラスト画像を含む多様な画像形式に対応できる柔軟性を実現している。

#### まとめ

PiXY は、マイクロアナリシスワークフローにおける粒子検出と座標変換のボトルネックを解決する実用的で拡張性の高いツールである。オープンソース化により、研究コミュニティによる改善とカスタマイズが容易になり、科学研究のスピードアップとコスト削減に貢献する。

**入手可能性**: PiXY は GitHub (https://github.com/YoshiakiKON/PiXY) で MIT ライセンスの下、無料のオープンソースソフトウェアとして入手可能である。スタンドアロンの Windows 実行ファイルは GitHub Releases で配布しており、ソースコードから Python 環境で実行することも可能である（クロスプラットフォーム対応）。

## AI利用の開示 (AI usage disclosure)

開発過程において AI 支援ペアプログラミング（GitHub Copilot、Google Gemini、xAI Grok、Anthropic Claude）を活用した。生成されたすべてのコードおよび文書は著者らにより確認・検証され、内容に対する全責任は著者に帰属する。

(加筆) AI ツールは主に文章の推敲案やコードの候補生成に用い、最終的な採否は著者が仕様・実行結果・目視確認に基づいて判断した。

---

## 謝辞（Acknowledgments）

本研究は、JSPS 科研費（課題番号：25H00682）の助成を受けて実施しました。また，Python、OpenCV、NumPy、PySide6 など、オープンソース科学計算エコシステムの開発者コミュニティに感謝いたします．

---

## 参考文献（References）

[1] Bradski, G. (2000). "The OpenCV library". Dr. Dobb's Journal of Software Tools, 25(11), 120-123.

[2] Harris, C. R., Millman, K. J., van der Walt, S. J., et al. (2020). "Array programming with NumPy". Nature, 585(7825), 357–362.

[3] Gonzalez, R. C., Woods, R. E., & Eddins, S. L. (2009). "Digital Image Processing Using MATLAB" (2nd ed.). Gatesmark Publishing.

[4] Lloyd, S. P. (1982). "Least squares quantization in PCM". IEEE Transactions on Information Theory, 28(2), 129–137.

[5] The Qt Company. (2024). "Qt for Python (PySide6)". Retrieved from https://wiki.qt.io/Qt_for_Python

[6] Rosten, E., & Drummond, T. (2006). "Machine Learning for High-Speed Corner Detection". In European Conference on Computer Vision (pp. 430-443). Springer.


